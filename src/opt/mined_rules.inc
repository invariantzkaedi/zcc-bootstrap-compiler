/* Auto-generated by tools/a100_transform_miner.py — DO NOT EDIT MANUALLY */
/* Formally verified sound via Z3 SMT-LIB2 BitVector Theorem Prover */

#ifndef ZCC_MINED_RULES_INC
#define ZCC_MINED_RULES_INC

/* imul_to_lea_shift_3: Replace IMUL by 3 with single-cycle shift-and-add (x << 1) + x (Saved: 2 cycles) */
bool ic_rule_synth_mul_3(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 3) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 1, it));
        rewrite_to_binop(c->fn, it, OP_ADD, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_5: Replace IMUL by 5 with single-cycle shift-and-add (x << 2) + x (Saved: 2 cycles) */
bool ic_rule_synth_mul_5(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 5) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 2, it));
        rewrite_to_binop(c->fn, it, OP_ADD, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_7: Replace IMUL by 7 with single-cycle shift-and-sub (x << 3) - x (Saved: 2 cycles) */
bool ic_rule_synth_mul_7(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 7) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 3, it));
        rewrite_to_binop(c->fn, it, OP_SUB, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_9: Replace IMUL by 9 with single-cycle shift-and-add (x << 3) + x (Saved: 2 cycles) */
bool ic_rule_synth_mul_9(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 9) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 3, it));
        rewrite_to_binop(c->fn, it, OP_ADD, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_15: Replace IMUL by 15 with single-cycle shift-and-sub (x << 4) - x (Saved: 2 cycles) */
bool ic_rule_synth_mul_15(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 15) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 4, it));
        rewrite_to_binop(c->fn, it, OP_SUB, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_17: Replace IMUL by 17 with single-cycle shift-and-add (x << 4) + x (Saved: 2 cycles) */
bool ic_rule_synth_mul_17(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 17) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 4, it));
        rewrite_to_binop(c->fn, it, OP_ADD, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_31: Replace IMUL by 31 with single-cycle shift-and-sub (x << 5) - x (Saved: 2 cycles) */
bool ic_rule_synth_mul_31(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 31) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 5, it));
        rewrite_to_binop(c->fn, it, OP_SUB, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_33: Replace IMUL by 33 with single-cycle shift-and-add (x << 5) + x (Saved: 2 cycles) */
bool ic_rule_synth_mul_33(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 33) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 5, it));
        rewrite_to_binop(c->fn, it, OP_ADD, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_63: Replace IMUL by 63 with single-cycle shift-and-sub (x << 6) - x (Saved: 2 cycles) */
bool ic_rule_synth_mul_63(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 63) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 6, it));
        rewrite_to_binop(c->fn, it, OP_SUB, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_65: Replace IMUL by 65 with single-cycle shift-and-add (x << 6) + x (Saved: 2 cycles) */
bool ic_rule_synth_mul_65(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 65) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 6, it));
        rewrite_to_binop(c->fn, it, OP_ADD, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_127: Replace IMUL by 127 with single-cycle shift-and-sub (x << 7) - x (Saved: 2 cycles) */
bool ic_rule_synth_mul_127(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 127) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 7, it));
        rewrite_to_binop(c->fn, it, OP_SUB, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_129: Replace IMUL by 129 with single-cycle shift-and-add (x << 7) + x (Saved: 2 cycles) */
bool ic_rule_synth_mul_129(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 129) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 7, it));
        rewrite_to_binop(c->fn, it, OP_ADD, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_255: Replace IMUL by 255 with single-cycle shift-and-sub (x << 8) - x (Saved: 2 cycles) */
bool ic_rule_synth_mul_255(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 255) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 8, it));
        rewrite_to_binop(c->fn, it, OP_SUB, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* imul_to_lea_shift_257: Replace IMUL by 257 with single-cycle shift-and-add (x << 8) + x (Saved: 2 cycles) */
bool ic_rule_synth_mul_257(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_MUL) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 257) {
        int shl_reg = make_binop(c->fn, OP_SHL, it->lhs, make_const(c->fn, it->type, 8, it));
        rewrite_to_binop(c->fn, it, OP_ADD, shl_reg, it->lhs);
        return true;
    }
    return false;
}

/* umod_pow2_2_to_and: Replace unsigned modulus by 2 with bitwise AND mask 0x1 (Saved: 13 cycles) */
bool ic_rule_synth_umod_2(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 2) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 1, it));
        return true;
    }
    return false;
}

/* umod_pow2_4_to_and: Replace unsigned modulus by 4 with bitwise AND mask 0x3 (Saved: 13 cycles) */
bool ic_rule_synth_umod_4(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 4) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 3, it));
        return true;
    }
    return false;
}

/* umod_pow2_8_to_and: Replace unsigned modulus by 8 with bitwise AND mask 0x7 (Saved: 13 cycles) */
bool ic_rule_synth_umod_8(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 8) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 7, it));
        return true;
    }
    return false;
}

/* umod_pow2_16_to_and: Replace unsigned modulus by 16 with bitwise AND mask 0xF (Saved: 13 cycles) */
bool ic_rule_synth_umod_16(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 16) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 15, it));
        return true;
    }
    return false;
}

/* umod_pow2_32_to_and: Replace unsigned modulus by 32 with bitwise AND mask 0x1F (Saved: 13 cycles) */
bool ic_rule_synth_umod_32(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 32) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 31, it));
        return true;
    }
    return false;
}

/* umod_pow2_64_to_and: Replace unsigned modulus by 64 with bitwise AND mask 0x3F (Saved: 13 cycles) */
bool ic_rule_synth_umod_64(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 64) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 63, it));
        return true;
    }
    return false;
}

/* umod_pow2_128_to_and: Replace unsigned modulus by 128 with bitwise AND mask 0x7F (Saved: 13 cycles) */
bool ic_rule_synth_umod_128(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 128) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 127, it));
        return true;
    }
    return false;
}

/* umod_pow2_256_to_and: Replace unsigned modulus by 256 with bitwise AND mask 0xFF (Saved: 13 cycles) */
bool ic_rule_synth_umod_256(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 256) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 255, it));
        return true;
    }
    return false;
}

/* umod_pow2_512_to_and: Replace unsigned modulus by 512 with bitwise AND mask 0x1FF (Saved: 13 cycles) */
bool ic_rule_synth_umod_512(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 512) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 511, it));
        return true;
    }
    return false;
}

/* umod_pow2_1024_to_and: Replace unsigned modulus by 1024 with bitwise AND mask 0x3FF (Saved: 13 cycles) */
bool ic_rule_synth_umod_1024(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 1024) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 1023, it));
        return true;
    }
    return false;
}

/* umod_pow2_2048_to_and: Replace unsigned modulus by 2048 with bitwise AND mask 0x7FF (Saved: 13 cycles) */
bool ic_rule_synth_umod_2048(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 2048) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 2047, it));
        return true;
    }
    return false;
}

/* umod_pow2_4096_to_and: Replace unsigned modulus by 4096 with bitwise AND mask 0xFFF (Saved: 13 cycles) */
bool ic_rule_synth_umod_4096(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 4096) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 4095, it));
        return true;
    }
    return false;
}

/* umod_pow2_8192_to_and: Replace unsigned modulus by 8192 with bitwise AND mask 0x1FFF (Saved: 13 cycles) */
bool ic_rule_synth_umod_8192(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 8192) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 8191, it));
        return true;
    }
    return false;
}

/* umod_pow2_16384_to_and: Replace unsigned modulus by 16384 with bitwise AND mask 0x3FFF (Saved: 13 cycles) */
bool ic_rule_synth_umod_16384(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 16384) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 16383, it));
        return true;
    }
    return false;
}

/* umod_pow2_32768_to_and: Replace unsigned modulus by 32768 with bitwise AND mask 0x7FFF (Saved: 13 cycles) */
bool ic_rule_synth_umod_32768(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 32768) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 32767, it));
        return true;
    }
    return false;
}

/* umod_pow2_65536_to_and: Replace unsigned modulus by 65536 with bitwise AND mask 0xFFFF (Saved: 13 cycles) */
bool ic_rule_synth_umod_65536(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_UMOD && it->op != OP_MOD) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == 65536) {
        rewrite_to_binop(c->fn, it, OP_BAND, it->lhs, make_const(c->fn, it->type, 65535, it));
        return true;
    }
    return false;
}

/* branchless_sign_sar63: Replace ternary (x < 0) ? -1 : 0 with arithmetic right shift (x >> 63) (Saved: 3 cycles) */
bool ic_rule_synth_sign_sar63(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_SELECT) return false;
    // (x < 0 ? -1 : 0) -> SAR x, 63
    int64_t t_val, f_val;
    if (reg_is_const(c->fn, it->lhs, &t_val) && reg_is_const(c->fn, it->rhs, &f_val)) {
        if (t_val == -1 && f_val == 0) {
            rewrite_to_binop(c->fn, it, OP_SAR, it->cond, make_const(c->fn, it->type, 63, it));
            return true;
        }
    }
    return false;
}

/* bitwise_absorption_or_and_not: Simplify ((x | 0xFF00) & ~0xFF00) to (x & ~0xFF00) (Saved: 1 cycles) */
bool ic_rule_synth_or_and_not(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_BAND) return false;
    int64_t k;
    if (!reg_is_const(c->fn, it->rhs, &k)) return false;
    Instr *def = def_of(c->fn, it->lhs);
    if (def && def->op == OP_BOR) {
        int64_t k2;
        if (reg_is_const(c->fn, def->rhs, &k2) && (k & k2) == 0) {
            rewrite_to_binop(c->fn, it, OP_BAND, def->lhs, it->rhs);
            return true;
        }
    }
    return false;
}

/* xor_minus_one_to_bnot: Simplify x ^ -1 (or x ^ ~0) into native single-byte bitwise NOT (~x) (Saved: 1 cycles) */
bool ic_rule_synth_xor_all_ones_to_not(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_XOR) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k == -1) {
        rewrite_to_unop(c->fn, it, OP_BNOT, it->lhs);
        return true;
    }
    return false;
}

/* double_bnot_elimination: Eliminate redundant double inversion ~(~x) to copy (x) (Saved: 2 cycles) */
bool ic_rule_synth_double_bnot(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_BNOT) return false;
    Instr *def = def_of(c->fn, it->lhs);
    if (def && def->op == OP_BNOT) {
        rewrite_to_copy(c->fn, it, def->lhs);
        return true;
    }
    return false;
}

/* sub_neg_to_add: Simplify x - (-C) into single-cycle addition x + C (Saved: 1 cycles) */
bool ic_rule_synth_sub_neg_to_add(ICtx *c) {
    Instr *it = c->it;
    if (it->op != OP_SUB) return false;
    int64_t k;
    if (reg_is_const(c->fn, it->rhs, &k) && k != (int64_t)0x8000000000000000ULL) {
        rewrite_to_binop(c->fn, it, OP_ADD, it->lhs, make_const(c->fn, it->type, -k, it));
        return true;
    }
    return false;
}

static bool run_all_mined_transforms(ICtx *c) {
    if (ic_rule_synth_mul_3(c)) return true;
    if (ic_rule_synth_mul_5(c)) return true;
    if (ic_rule_synth_mul_7(c)) return true;
    if (ic_rule_synth_mul_9(c)) return true;
    if (ic_rule_synth_mul_15(c)) return true;
    if (ic_rule_synth_mul_17(c)) return true;
    if (ic_rule_synth_mul_31(c)) return true;
    if (ic_rule_synth_mul_33(c)) return true;
    if (ic_rule_synth_mul_63(c)) return true;
    if (ic_rule_synth_mul_65(c)) return true;
    if (ic_rule_synth_mul_127(c)) return true;
    if (ic_rule_synth_mul_129(c)) return true;
    if (ic_rule_synth_mul_255(c)) return true;
    if (ic_rule_synth_mul_257(c)) return true;
    if (ic_rule_synth_umod_2(c)) return true;
    if (ic_rule_synth_umod_4(c)) return true;
    if (ic_rule_synth_umod_8(c)) return true;
    if (ic_rule_synth_umod_16(c)) return true;
    if (ic_rule_synth_umod_32(c)) return true;
    if (ic_rule_synth_umod_64(c)) return true;
    if (ic_rule_synth_umod_128(c)) return true;
    if (ic_rule_synth_umod_256(c)) return true;
    if (ic_rule_synth_umod_512(c)) return true;
    if (ic_rule_synth_umod_1024(c)) return true;
    if (ic_rule_synth_umod_2048(c)) return true;
    if (ic_rule_synth_umod_4096(c)) return true;
    if (ic_rule_synth_umod_8192(c)) return true;
    if (ic_rule_synth_umod_16384(c)) return true;
    if (ic_rule_synth_umod_32768(c)) return true;
    if (ic_rule_synth_umod_65536(c)) return true;
    if (ic_rule_synth_sign_sar63(c)) return true;
    if (ic_rule_synth_or_and_not(c)) return true;
    if (ic_rule_synth_xor_all_ones_to_not(c)) return true;
    if (ic_rule_synth_double_bnot(c)) return true;
    if (ic_rule_synth_sub_neg_to_add(c)) return true;
    return false;
}

#endif /* ZCC_MINED_RULES_INC */

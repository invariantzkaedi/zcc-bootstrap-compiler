feat(lab): integrate zkaedi-lab configuration evolution sandbox

Goal:
Add the Tier-0 zkaedi-lab configuration evolution sandbox and harden it to run cleanly in the current WSL sandbox environment.

Outcome:
The sandbox files are integrated. Sandbox policy unshare isolation updated to use unprivileged user namespaces ('unshare -rn') and hardcoded home paths dynamically remap to REPO root. Selftest passes 14/14.
